server:
  port: 9002
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userService
    username: root
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update      
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
    generate-ddl: true