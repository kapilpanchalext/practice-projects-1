server:
  port: 9001
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: REDIS-DEMO
  datasource:
    url: jdbc:postgresql://localhost:5432/appjforcecat
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: true